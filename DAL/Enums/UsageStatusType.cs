namespace DAL.Enums
{
    public enum UsageStatusType
    {
        Frequent = 0,     // Dùng thường xuyên
        Occasionally = 1, // Dùng thỉnh thoảng
        Seldom = 2,       // Ít dùng
        Unused = 3,       // Không dùng nữa  
        NotEvaluated = 4  // Chưa đánh giá
    }
}